{
    "extends": ["../../tsconfig.json"],

    "include": ["src", "**/*.d.ts", "./*.config.ts"],
    "exclude": ["**/dist", "**/node_modules"],

    "compilerOptions": {
        "esModuleInterop": true,
        "types": ["bun-types"],

        "paths": {
            "@env": ["./src/env.ts"]
        }
    }
}
